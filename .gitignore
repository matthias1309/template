# Claude Code local overrides — personal preferences, not for the team
CLAUDE.local.md

# Personal permission overrides — often end up containing secrets from ad-hoc commands
.claude/settings.local.json

# Common project artifacts (extend as needed)
node_modules/
dist/
build/
.env
.env.*
!.env.example
*.env
*.log
